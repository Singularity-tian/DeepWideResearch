# Chat Interface env template
# Copy to .env.local in the same directory
# cp chat_interface/env.example chat_interface/.env.local

# Backend API base URL (FastAPI server)
NEXT_PUBLIC_API_URL=http://localhost:8000
